# SPARK Application Data

This directory holds application-level runtime fixtures and configuration data.

## Guidelines
- Raw video archives and large extracted frame sets are stored externally and are **not** committed to version control.
- Any temporary video uploads are stored in `backend/storage/uploads/` and are automatically ignored by Git.
